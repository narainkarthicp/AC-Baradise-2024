// Subscription page widget

import 'package:flutter/material.dart';

class SubcriptionScheme extends StatelessWidget {
  const SubcriptionScheme({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Column(
          children: [
            Row(
            )
          ],
        ),
      ),
    );
  }
}